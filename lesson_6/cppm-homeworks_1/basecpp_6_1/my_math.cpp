#include "my_math.h"
#include <math.h>
int sum(int x, int y)
{
	return x + y;
}

int sub(int x, int y)
{
	return x - y;
}

int multi(int x, int y)
{
	return x * y;
}

int div1(int x, int y)
{
	return x / y;
}

int exp(int x, int y)
{
	return pow(x, y);
}

